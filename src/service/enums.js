
const EErrors = {
    INVALID_TYPES_ERROR :1, // espera un String y recibe un Number
    INVALID_FORMAT_ERROR:2, //un dato qe no se esta pasando ( undefinded)
    DATABASE_ERROR:3, //ocurrio un error al guardar el producto en la base de datos
    ROUTING_ERROR:4, //
}

export default EErrors